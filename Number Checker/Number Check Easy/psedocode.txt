BEGIN

list <- [1,2,4,8,16,32,64,128]
size <- 8
found <- 0

PRINT "Enter a number to check: "
READ target
FOR i=0 TO size -1 do
    FOR j=i+1 TO size -1 DO 
        IF (list[i] + list[j]) = target
            found <- 1
            EXIT both loops
        ENDIF
    ENDFOR
ENDFOR

IF found=1 THEN
        PRINT "There are two numbers in the list summing to the keyed-in number", target
    ELSE
        PRINT "There are not two numbers in the list summing to the keyed-in number", target
    ENDIF
END

















END